import { extendTheme } from '@mui/joy/styles'

export const darkTheme = extendTheme({
    colorSchemes: {
        light: {
            palette: {},
        },
        dark: {
            palette: {},
        },
    },
})
